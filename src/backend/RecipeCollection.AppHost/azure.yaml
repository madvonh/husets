# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: recipecollection-apphost
services:  
  app:
    language: dotnet
    project: ./RecipeCollection.AppHost.csproj
    host: containerapp
